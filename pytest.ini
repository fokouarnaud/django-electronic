[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = test_*.py
testpaths = tests
addopts = -ra
# Upcoming Django behaviour changes fail the suite instead of scrolling by.
filterwarnings =
    error::django.utils.deprecation.RemovedInDjango60Warning
